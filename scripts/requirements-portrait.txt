# Requires Python 3.10-3.12 (rembg pins <3.13). Build with:
#   python3.12 -m venv .venv-portrait && .venv-portrait/bin/pip install -r scripts/requirements-portrait.txt

# Portrait pipeline only. Run locally when the source photo changes;
# never installed by the daily workflow (rembg pulls ONNX Runtime + a model
# download, which would turn a 20-second cron job into a multi-minute one).
-r requirements.txt
pillow==11.0.0
numpy==2.1.3
opencv-python==4.10.0.84
rembg==2.0.59
